using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class keySwitch : MonoBehaviour
{
     
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {/*
        if (Input.GetKeyDown("space"))
        {
                Debug.Log(SceneManager.GetActiveScene().name);
            if(SceneManager.GetActiveScene().name == "relaxationIntro")
            {
                scene.num = 1;
                Application.LoadLevel("loft_1");
            }

            else if (SceneManager.GetActiveScene().name == "relaxationSuite")
            {
                scene.num++;
                Application.LoadLevel("loft_"+scene.num);
            }

            else
            {
                Application.LoadLevel("relaxationSuite");
            }

        }*/
    }
}
